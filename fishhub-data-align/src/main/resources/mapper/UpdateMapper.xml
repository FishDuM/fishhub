<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="hk.ljx.fishhub.data.align.domain.mapper.UpdateMapper">

    <update id="updateUserFollowingTotalByUserId" parameterType="map">
        insert into t_user_count (user_id, following_total) values (#{userId}, #{followingTotal})
        on duplicate key update following_total = values(following_total)
    </update>

    <update id="updateUserFansTotalByUserId" parameterType="map">
        insert into t_user_count (user_id, fans_total) values (#{userId}, #{fansTotal})
        on duplicate key update fans_total = values(fans_total)
    </update>

    <update id="updateUserLikeTotalByUserId" parameterType="map">
        insert into t_user_count (user_id, like_total) values (#{userId}, #{likeTotal})
        on duplicate key update like_total = values(like_total)
    </update>

    <update id="updateUserCollectTotalByUserId" parameterType="map">
        insert into t_user_count (user_id, collect_total) values (#{userId}, #{collectTotal})
        on duplicate key update collect_total = values(collect_total)
    </update>

    <update id="updateUserNoteTotalByUserId" parameterType="map">
        insert into t_user_count (user_id, note_total) values (#{userId}, #{noteTotal})
        on duplicate key update note_total = values(note_total)
    </update>

    <update id="updateNoteLikeTotalByUserId" parameterType="map">
        insert into t_note_count (note_id, like_total) values (#{noteId}, #{noteLikeTotal})
        on duplicate key update like_total = values(like_total)
    </update>

    <update id="updateNoteCollectTotalByUserId" parameterType="map">
        insert into t_note_count (note_id, collect_total) values (#{noteId}, #{noteCollectTotal})
        on duplicate key update collect_total = values(collect_total)
    </update>

</mapper>
